{
	"name": "lazy-load",
	"version": "1.0.0",
	"description": "Lazy load görseller",
	"scripts": {
		"build": "parcel build src/lazy-load.js --dist-dir dist --no-source-maps"
	},
	"keywords": [
		"js",
		"lazy",
		"load",
		"lazy load"
	],
	"author": "drementer",
	"license": "MIT",
	"devDependencies": {
		"parcel": "^2.5.0"
	}
}
